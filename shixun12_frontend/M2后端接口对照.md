The user wants me to analyze all the Controller files and map them to the 18 M2 frontend methods. Let me read all the relevant controller files first, then do a thorough analysis.
Now let me also read the DTO and entity files that are referenced in the controllers to understand the data structures. I need to read:
1. TeamRequestDTO - for M4 team requests
2. ExitVoteCreateDTO, ExitVoteSubmitDTO, ExitVote - for exit vote
3. SubmitEvaluationRequest, EvaluatableMemberDTO, EvaluationSubmitResult, ReceivedEvaluationVO - for M5 evaluation
4. CreditScoreVO, CreditChangeVO, CreditChangePageVO - for M5 credit
5. ReportCreateDTO, Report - for M6 reports
6. AppealCreateDTO, Appeal - for M6 appeals

Also need to read the ReasonCode enum for error codes.

Let me also check if there's a `getAppealTargets` endpoint that I might have missed - I didn't see it in the AppealController. Let me search.
Now let me read the remaining DTOs and entities needed:
- CreditScoreVO, CreditChangeVO, CreditChangePageVO
- ReportCreateDTO, Report entity
- AppealCreateDTO, Appeal entity
- ReasonCode enum
Now I need to check for method 16: `getAppealTargets(type)` - looking for an endpoint that returns appeal targets. I don't see this in AppealController.java. Let me search for it.

Also, I need to check the `getRequestList` return type - the controller returns `Result<?>` and calls `teamRequestService.getRequestList(userId, direction)`. Let me check the TeamRequestService.

Let me also check if there are any other controllers that might have appeal targets.
Now I have a comprehensive understanding. Let me also read the TeamRequest entity to understand the getRequestList return type.
Now I have all the information I need. Let me also search for any "get targets" or "can-appeal" endpoints that might relate to method 16 (getAppealTargets).
There is no `getAppealTargets` endpoint in any controller. Method 16 will be marked as "⚠️ 未找到". 

Now let me compile the comprehensive Markdown report for all 18 methods. I have all the data I need.

Let me organize this properly now.
以下是完整的 **M2 前端方法 ↔ 后端接口对照清单**：

---

# TeamMatch 后端接口对照清单（M2 前端适配版）

> 所有路径前缀 `/api`，统一返回 `Result<T>`（`code` + `message` + `data`）
> 鉴权方式：JWT token → `Authorization` 请求头

---

## M4 组队与退出模块

### 编号 1：getRequests(type) — 获取组队请求列表

| 项目 | 内容 |
|------|------|
| **后端 Controller** | `TeamRequestController.java` |
| **Controller 方法名** | `getRequestList` |
| **请求方式** | `GET` |
| **完整路径** | `/api/m4/team-requests` |
| **路径参数** | 无 |
| **Query 参数** | `userId` (Long, 必填), `direction` (String, 默认 `"received"`, 可选值 `received` / `sent`) |
| **Body 参数** | 无 |
| **成功响应 data 字段** | `List<TeamRequest>`：`id`(Long), `projectId`(Long), `fromUserId`(Long), `toUserId`(Long), `requestType`(String: invite/apply), `status`(String: pending/accepted/rejected/cancelled/expired), `message`(String), `handledAt`(LocalDateTime), `createdAt`(LocalDateTime) |
| **可能的错误码** | 无显式错误码 |

> **适配说明**：前端 `type` → 后端 `direction`，`"received"` 表示收到的请求，`"sent"` 表示发出的请求

---

### 编号 2：acceptRequest(id) — 接受组队请求

| 项目 | 内容 |
|------|------|
| **后端 Controller** | `TeamRequestController.java` |
| **Controller 方法名** | `acceptRequest` |
| **请求方式** | `POST` |
| **完整路径** | `/api/m4/team-requests/{id}/accept` |
| **路径参数** | `id` (Long) — 请求ID |
| **Query 参数** | `operatorId` (Long, 必填) — 操作者 userId |
| **Body 参数** | 无 |
| **成功响应 data 字段** | `null` (Void) |
| **可能的错误码** | `M1003` STATUS_CONFLICT — 状态冲突 |

---

### 编号 3：rejectRequest(id) — 拒绝组队请求

| 项目 | 内容 |
|------|------|
| **后端 Controller** | `TeamRequestController.java` |
| **Controller 方法名** | `rejectRequest` |
| **请求方式** | `POST` |
| **完整路径** | `/api/m4/team-requests/{id}/reject` |
| **路径参数** | `id` (Long) — 请求ID |
| **Query 参数** | `operatorId` (Long, 必填) — 操作者 userId |
| **Body 参数** | 无 |
| **成功响应 data 字段** | `null` (Void) |
| **可能的错误码** | `M1003` STATUS_CONFLICT — 状态冲突 |

---

### 编号 4：cancelRequest(id) — 取消组队请求

| 项目 | 内容 |
|------|------|
| **后端 Controller** | `TeamRequestController.java` |
| **Controller 方法名** | `cancelRequest` |
| **请求方式** | `POST` |
| **完整路径** | `/api/m4/team-requests/{id}/cancel` |
| **路径参数** | `id` (Long) — 请求ID |
| **Query 参数** | `operatorId` (Long, 必填) — 操作者 userId |
| **Body 参数** | 无 |
| **成功响应 data 字段** | `null` (Void) |
| **可能的错误码** | `M4005` M4_RESOURCE_NOT_FOUND — 请求不存在 |

---

### 编号 5：getMembers(projectId) — 获取项目成员列表

| 项目 | 内容 |
|------|------|
| **后端 Controller** | `ProjectController.java` |
| **Controller 方法名** | `getMembers` |
| **请求方式** | `GET` |
| **完整路径** | `/api/m4/projects/{id}/members` |
| **路径参数** | `id` (Long) — 项目ID |
| **Query 参数** | 无 |
| **Body 参数** | 无 |
| **成功响应 data 字段** | `List<TeamMember>`：由 `teamMemberService.getProjectMembers(id)` 返回 |
| **可能的错误码** | 无显式错误码 |

---

### 编号 6：selfExit(projectId, reason) — 成员主动退出

| 项目 | 内容 |
|------|------|
| **后端 Controller** | `ExitVoteController.java` |
| **Controller 方法名** | `selfExit` |
| **请求方式** | `POST` |
| **完整路径** | `/api/m4/projects/{projectId}/exit/self` |
| **路径参数** | `projectId` (Long) — 项目ID |
| **Query 参数** | `userId` (Long, 必填) — 退出者 userId |
| **Body 参数** | 无 |
| **成功响应 data 字段** | `null` (Void) |
| **可能的错误码** | `M4001` M4_PROJECT_NOT_FOUND、`M4009` M4_LEADER_CANNOT_EXIT、`M4004` M4_MEMBER_NOT_ACTIVE、`TEAM_VOTE_CONFLICT`、`M9999` |

> **适配说明**：后端没有独立的 `reason` 字段（`selfExit` 是直接退出，无审批），如需附带原因请扩展

---

### 编号 7：getExitVoteDetail(voteId) — 获取退出投票详情

| 项目 | 内容 |
|------|------|
| **后端 Controller** | `ExitVoteController.java` |
| **Controller 方法名** | `getVoteDetail` |
| **请求方式** | `GET` |
| **完整路径** | `/api/m4/projects/{projectId}/exit/votes/{voteId}` |
| **路径参数** | `projectId` (Long), `voteId` (Long) |
| **Query 参数** | 无 |
| **Body 参数** | 无 |
| **成功响应 data 字段** | `ExitVote`：`id`(Long), `projectId`(Long), `targetUserId`(Long), `initiatorId`(Long), `status`(String: voting/closed), `penaltyLevel`(String: negotiated/malicious), `result`(String: pass/reject/null), `reason`(String), `totalVoters`(Integer), `agreeCount`(Integer), `disagreeCount`(Integer), `deadlineAt`(LocalDateTime), `closedAt`(LocalDateTime), `createdAt`(LocalDateTime) |
| **可能的错误码** | `M4005` M4_RESOURCE_NOT_FOUND |

---

### 编号 8：submitExitVote(voteId, choice) — 提交投票

| 项目 | 内容 |
|------|------|
| **后端 Controller** | `ExitVoteController.java` |
| **Controller 方法名** | `submitVote` |
| **请求方式** | `POST` |
| **完整路径** | `/api/m4/projects/{projectId}/exit/votes/{voteId}/submit` |
| **路径参数** | `projectId` (Long), `voteId` (Long) |
| **Query 参数** | 无 |
| **Body 参数** | `ExitVoteSubmitDTO`：`voterId`(Long), `choice`(String: agree/disagree) |
| **成功响应 data 字段** | `null` (Void) |
| **可能的错误码** | `M4005` M4_RESOURCE_NOT_FOUND、`M4008` M4_VOTE_ALREADY_CLOSED、`M4006` M4_DUPLICATE_OPERATION、`M4004` M4_MEMBER_NOT_ACTIVE、`M1003` STATUS_CONFLICT |

---

### 编号 9：closeExitVote(voteId) — 队长关闭投票

| 项目 | 内容 |
|------|------|
| **后端 Controller** | `ExitVoteController.java` |
| **Controller 方法名** | `closeVote` |
| **请求方式** | `POST` |
| **完整路径** | `/api/m4/projects/{projectId}/exit/votes/{voteId}/close` |
| **路径参数** | `projectId` (Long), `voteId` (Long) |
| **Query 参数** | `operatorId` (Long, 必填) — 队长 userId |
| **Body 参数** | 无 |
| **成功响应 data 字段** | `ExitVote` — 关闭后的投票详情（同上 ExitVote 字段） |
| **可能的错误码** | `M4005` M4_RESOURCE_NOT_FOUND、`M4002` M4_NOT_LEADER |

---

## M5 互评与信誉模块

### 编号 10：checkEvaluationEligibility(projectId, targetUserId) — 检查互评资格

| 项目 | 内容 |
|------|------|
| **后端 Controller** | `EvaluationController.java` |
| **Controller 方法名** | `checkTargetEligibility` |
| **请求方式** | `GET` |
| **完整路径** | `/api/m5/projects/{projectId}/members/{targetId}/evaluation-eligibility` |
| **路径参数** | `projectId` (Long), `targetId` (Long) — 即 `targetUserId` |
| **Query 参数** | 无 |
| **Body 参数** | 无 |
| **鉴权** | `Authorization` 请求头 (JWT token) |
| **成功响应 data 字段** | `Boolean` — `true` 有资格，`false` 带有具体 code/message |
| **可能的错误码** | `SELF_EVALUATION`、`ALREADY_EVALUATED`、`NOT_PROJECT_MEMBER`、`PROJECT_NOT_ENDED`、`EVAL_WINDOW_CLOSED`、`TARGET_NOT_PROJECT_MEMBER` |

> **配套接口**：另有 `GET /api/m5/projects/{projectId}/evaluation-eligibility`（项目级资格检查）和 `GET /api/m5/projects/{projectId}/evaluatable-members`（可评价成员列表）

---

### 编号 11：submitEvaluation(params) — 提交互评

| 项目 | 内容 |
|------|------|
| **后端 Controller** | `EvaluationController.java` |
| **Controller 方法名** | `submitEvaluation` |
| **请求方式** | `POST` |
| **完整路径** | `/api/m5/evaluations` |
| **路径参数** | 无 |
| **Query 参数** | 无 |
| **Body 参数** | `SubmitEvaluationRequest`：`projectId`(Long), `targetId`(Long), `communicationScore`(Integer, 1-5), `taskScore`(Integer, 1-5), `skillScore`(Integer, 1-5), `responsibilityScore`(Integer, 1-5), `comment`(String), `positiveTags`(List\<String\>), `negativeTags`(List\<String\>) |
| **鉴权** | `Authorization` 请求头 (评价人身份从 token 注入，不可伪造) |
| **成功响应 data 字段** | `EvaluationSubmitResult`：`evaluationId`(Long), `evaluationStatus`(String: normal/pending_review/kept_no_credit), `effective`(Boolean), `creditResult`(CreditCalculationResult), `anomalyResult`(AnomalyDetectionResult) |
| **可能的错误码** | `M5201`~`M5206` 内容校验错误、资格类错误（同上） |

---

### 编号 12：getCurrentCreditScore() — 获取当前信誉分

| 项目 | 内容 |
|------|------|
| **后端 Controller** | `CreditController.java` |
| **Controller 方法名** | `getMyCreditScore` |
| **请求方式** | `GET` |
| **完整路径** | `/api/m5/credit/score` |
| **路径参数** | 无 |
| **Query 参数** | 无 |
| **Body 参数** | 无 |
| **鉴权** | `Authorization` 请求头 (JWT token) |
| **成功响应 data 字段** | `CreditScoreVO`：`userId`(Long), `creditScore`(Integer) |
| **可能的错误码** | `M3000` UNAUTHORIZED、`M1002` NOT_FOUND |

---

### 编号 13：getCreditHistory() — 获取信誉分变化记录

| 项目 | 内容 |
|------|------|
| **后端 Controller** | `CreditController.java` |
| **Controller 方法名** | `getMyCreditChanges` |
| **请求方式** | `GET` |
| **完整路径** | `/api/m5/credit/changes` |
| **路径参数** | 无 |
| **Query 参数** | `projectId`(Long, 可选), `changeType`(String, 可选: evaluation/exit_vote/self_exit/penalty/penalty_restore/appeal_restore), `page`(int, 默认1), `pageSize`(int, 默认20, 最大100) |
| **Body 参数** | 无 |
| **鉴权** | `Authorization` 请求头 |
| **成功响应 data 字段** | `CreditChangePageVO`：`list`(List\<CreditChangeVO\>), `total`(long), `page`(int), `pageSize`(int)。`CreditChangeVO`：`id`(Long), `userId`(Long), `projectId`(Long), `changeType`(String), `changeValue`(Integer), `effective`(Boolean), `suspended`(Boolean), `description`(String), `createdAt`(LocalDateTime) |
| **可能的错误码** | `M3000` UNAUTHORIZED、`M1001` PARAM_ERROR（changeType 不合法时） |

---

## M6 举报与申诉模块

### 编号 14：submitReport(params) — 提交举报

| 项目 | 内容 |
|------|------|
| **后端 Controller** | `ReportController.java` |
| **Controller 方法名** | `createReport` |
| **请求方式** | `POST` |
| **完整路径** | `/api/reports` |
| **路径参数** | 无 |
| **Query 参数** | 无 |
| **Body 参数** | `ReportCreateDTO`：`targetType`(String, 必填, 仅限 `user` 或 `project`), `targetId`(Long, 必填), `reason`(String, 必填, 1-500字符) |
| **鉴权** | `Authorization` 请求头 (必填) |
| **成功响应 data 字段** | `Report`：`id`(Long), `reporterId`(Long), `targetType`(String), `targetId`(Long), `reason`(String), `status`(String: pending/resolved/dismissed), `handlerId`(Long), `handleResult`(String), `handledAt`(LocalDateTime), `createdAt`(LocalDateTime) |
| **可能的错误码** | `M1001` PARAM_ERROR |

---

### 编号 15：getMyReports() — 获取我的举报列表

| 项目 | 内容 |
|------|------|
| **后端 Controller** | `ReportController.java` |
| **Controller 方法名** | `getMyReports` |
| **请求方式** | `GET` |
| **完整路径** | `/api/reports/my` |
| **路径参数** | 无 |
| **Query 参数** | 无 |
| **Body 参数** | 无 |
| **鉴权** | `Authorization` 请求头 (必填) |
| **成功响应 data 字段** | `List<Report>` — 同上 Report 字段列表 |
| **可能的错误码** | `M3000` UNAUTHORIZED |

---

### 编号 16：getAppealTargets(type) — 获取可申诉对象 ⚠️ 未找到

| 项目 | 内容 |
|------|------|
| **后端 Controller** | ❌ 无对应接口 |
| **说明** | 后端 `AppealController` 和 `AppealService` 中**不存在** `getAppealTargets` 方法。目前后端只提供 `GET /appeals/{id}`（申诉详情）、`GET /appeals/my`（我的申诉列表）、`POST /appeals`（提交申诉）。如需此功能，前端可通过 `GET /api/m5/evaluations/received?projectId=xxx`（已收互评）+ `GET /api/penalty/my`（如存在处罚查询接口）自行拼装可申诉对象列表 |

---

### 编号 17：submitAppeal(params) — 提交申诉

| 项目 | 内容 |
|------|------|
| **后端 Controller** | `AppealController.java` |
| **Controller 方法名** | `createAppeal` |
| **请求方式** | `POST` |
| **完整路径** | `/api/appeals` |
| **路径参数** | 无 |
| **Query 参数** | 无 |
| **Body 参数** | `AppealCreateDTO`：`targetType`(String, 必填, `evaluation` 或 `penalty`), `targetId`(Long, 必填), `reason`(String, 必填) |
| **鉴权** | `Authorization` 请求头 (必填) |
| **成功响应 data 字段** | `Appeal`：`id`(Long), `userId`(Long), `targetType`(String), `targetId`(Long), `reason`(String), `status`(String: pending/approved/rejected), `handlerId`(Long), `handleResult`(String), `handledAt`(LocalDateTime), `createdAt`(LocalDateTime) |
| **可能的错误码** | `M1001` PARAM_ERROR、`M1003` STATUS_CONFLICT（已有待处理申诉） |

---

### 编号 18：getMyAppeals() — 获取我的申诉列表

| 项目 | 内容 |
|------|------|
| **后端 Controller** | `AppealController.java` |
| **Controller 方法名** | `getMyAppeals` |
| **请求方式** | `GET` |
| **完整路径** | `/api/appeals/my` |
| **路径参数** | 无 |
| **Query 参数** | 无 |
| **Body 参数** | 无 |
| **鉴权** | `Authorization` 请求头 (必填) |
| **成功响应 data 字段** | `List<Appeal>` — 同上 Appeal 字段列表，按时间倒序 |
| **可能的错误码** | `M3000` UNAUTHORIZED |

---

## 汇总

| 编号 | 前端方法 | 后端接口 | 状态 |
|------|----------|----------|------|
| 1 | `getRequests(type)` | `GET /api/m4/team-requests?userId=&direction=` | ✅ |
| 2 | `acceptRequest(id)` | `POST /api/m4/team-requests/{id}/accept?operatorId=` | ✅ |
| 3 | `rejectRequest(id)` | `POST /api/m4/team-requests/{id}/reject?operatorId=` | ✅ |
| 4 | `cancelRequest(id)` | `POST /api/m4/team-requests/{id}/cancel?operatorId=` | ✅ |
| 5 | `getMembers(projectId)` | `GET /api/m4/projects/{id}/members` | ✅ |
| 6 | `selfExit(projectId, reason)` | `POST /api/m4/projects/{projectId}/exit/self?userId=` | ✅ |
| 7 | `getExitVoteDetail(voteId)` | `GET /api/m4/projects/{projectId}/exit/votes/{voteId}` | ✅ |
| 8 | `submitExitVote(voteId, choice)` | `POST /api/m4/projects/{projectId}/exit/votes/{voteId}/submit` | ✅ |
| 9 | `closeExitVote(voteId)` | `POST /api/m4/projects/{projectId}/exit/votes/{voteId}/close?operatorId=` | ✅ |
| 10 | `checkEvaluationEligibility(...)` | `GET /api/m5/projects/{projectId}/members/{targetId}/evaluation-eligibility` | ✅ |
| 11 | `submitEvaluation(params)` | `POST /api/m5/evaluations` | ✅ |
| 12 | `getCurrentCreditScore()` | `GET /api/m5/credit/score` | ✅ |
| 13 | `getCreditHistory()` | `GET /api/m5/credit/changes` | ✅ |
| 14 | `submitReport(params)` | `POST /api/reports` | ✅ |
| 15 | `getMyReports()` | `GET /api/reports/my` | ✅ |
| 16 | `getAppealTargets(type)` | — | ⚠️ 未找到 |
| 17 | `submitAppeal(params)` | `POST /api/appeals` | ✅ |
| 18 | `getMyAppeals()` | `GET /api/appeals/my` | ✅ |

**17/18 已匹配，1 个缺失**（`getAppealTargets`），需前端自行组装或后端新增接口。